// Copy this file to secrets.h and edit with your values
// secrets.h is excluded from git via .gitignore
#pragma once

// Device hostname (visible on your network)
#define WIFI_HOSTNAME "ikea-led"

#ifdef ESP8266
// ESP8266 only — ESP32 uses WiFi Manager (captive portal)
#define WIFI_SSID "your-wifi-name"
#define WIFI_PASSWORD "your-wifi-password"
#endif

// OTA update credentials (http://<device-ip>/update)
#define OTA_USERNAME "admin"
#define OTA_PASSWORD "ikea-led-wall"
